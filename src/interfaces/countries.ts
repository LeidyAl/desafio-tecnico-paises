export interface country{
  name: string,
  capital: string, 
  code: string,
  continent:  string, 
  currency: string, 
  phone: string
}

export interface AllCountries{
  name: string;
  code: string;
  continent: string;
  currency: string;
}